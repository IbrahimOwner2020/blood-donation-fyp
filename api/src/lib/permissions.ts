/**
 * Permission / role name constants for requirePermission middleware.
 * Re-exports seed-canonical codes so routes never hardcode strings.
 */
export {
  PERMISSION_CODES,
  ROLE_NAMES,
  type PermissionCode,
  type RoleName,
} from '../db/seed/permission-codes'

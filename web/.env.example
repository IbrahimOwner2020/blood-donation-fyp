# Browser-facing API base (docs/15). Must match the Hono /api/v1 mount.
# Login uses credentials: 'include'; API must list this page's origin in APP_ORIGINS
# (http://localhost:5173 and/or http://localhost:5174) so CORS allows cookie auth.
# Demo login emails/passwords: see root README / .env.example (local only).
VITE_API_URL=http://localhost:3000/api/v1
